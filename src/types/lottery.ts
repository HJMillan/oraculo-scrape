// ─── Frontend type definitions ───
// Re-exports from the single source of truth in shared/types.ts

export type { ItemStatus, LotteryItem, LotterySection, ScrapingResponse } from '../../shared/types';
